# Day 03 – Database Integration with SQLAlchemy

## Overview

This folder contains my solutions and practice exercises completed during **Day 03** of **Week 02** of the internship training plan. The focus of this day was integrating **FastAPI** with **PostgreSQL** using **SQLAlchemy ORM**, performing CRUD operations, managing database sessions through dependency injection, creating relationships between models, organizing the project into a modular structure, and using **Alembic** for database migrations.

---

## Topics Covered

### SQLAlchemy Setup

* Installing SQLAlchemy
* Installing PostgreSQL driver (`psycopg2-binary`)
* Connecting FastAPI to PostgreSQL

### Database Configuration

* `DATABASE_URL`
* `create_engine()`
* `SessionLocal`
* `declarative_base()`

### SQLAlchemy Models

* Creating database models
* Defining columns with `Column`
* Using data types:
  * `Integer`
  * `String`
  * `Boolean`
  * `ForeignKey`

### Database Relationships

* One-to-Many relationships
* Many-to-One relationships
* `relationship()`
* `back_populates`

### Dependency Injection

* Database session management using `get_db()`
* Using `yield`
* `Depends(get_db)`

### CRUD Operations

* Create
* Read
* Read All
* Delete

Using:

* `db.add()`
* `db.commit()`
* `db.refresh()`
* `db.query()`
* `filter()`
* `first()`
* `all()`

### Pydantic Schemas

* Request schemas
* Response schemas
* ORM compatibility using:

```python
model_config = {
    "from_attributes": True
}
```

### SQLAlchemy Models vs Pydantic Schemas

* Database models
* Request validation
* Response serialization
* Separation of concerns

### Alembic Migrations

* Initializing Alembic
* Creating migrations
* Auto-generating migration files
* Applying database migrations

---

## Practice Questions Implemented

### 1. SQLAlchemy Setup with PostgreSQL

Configured SQLAlchemy with PostgreSQL, created the database connection, engine, session factory, and base class. Created the `User` model and generated database tables.

**Concepts Used**

* PostgreSQL
* SQLAlchemy
* `create_engine()`
* `SessionLocal`
* `declarative_base()`
* `Base.metadata.create_all()`

Files:

```text
database.py
models.py
main.py
```

---

### 2. Database Dependency & POST Endpoint

Implemented a reusable `get_db()` dependency using `yield` and injected it into the `POST /users` endpoint to create and store users in the database.

**Concepts Used**

* Dependency Injection
* `Depends()`
* `yield`
* `db.add()`
* `db.commit()`
* `db.refresh()`

Files:

```text
database.py
crud.py
routers/users.py
```

---

### 3. Get User by ID

Implemented a `GET /users/{user_id}` endpoint that retrieves a user from the database and returns **404 Not Found** if the user does not exist.

**Concepts Used**

* Querying with SQLAlchemy
* `filter()`
* `first()`
* `HTTPException`

Files:

```text
crud.py
routers/users.py
```

---

### 4. Get All Users

Implemented a `GET /users` endpoint that returns all users with database-level pagination using `skip` and `limit`.

**Concepts Used**

* Query Parameters
* Pagination
* `offset()`
* `limit()`
* `all()`

Files:

```text
crud.py
routers/users.py
```

---

### 5. Delete User

Implemented a `DELETE /users/{user_id}` endpoint that removes a user from the database and returns **204 No Content**.

**Concepts Used**

* Delete Operations
* `db.delete()`
* `db.commit()`
* HTTP Status Codes

Files:

```text
crud.py
routers/users.py
```

---

### 6. User & Post Relationship

Created a `Post` model with a foreign key linked to the `User` model. Established a one-to-many relationship using `relationship()` and implemented a `GET /users/{user_id}/posts` endpoint to retrieve all posts belonging to a specific user.

**Concepts Used**

* `ForeignKey`
* `relationship()`
* `back_populates`
* One-to-Many Relationships
* Database Queries

Files:

```text
models.py
schemas.py
crud.py
routers/users.py
```

---

### 7. Alembic Database Migrations

Initialized Alembic, configured it to use the project's SQLAlchemy models, generated the initial migration automatically, and applied the migration to the PostgreSQL database.

**Concepts Used**

* Alembic
* Database Migrations
* Autogenerated Revisions
* Schema Versioning

Commands Used:

```bash
alembic init alembic

alembic revision --autogenerate -m "create users and posts tables"

alembic upgrade head
```

---

## PostgreSQL Database


<img width="1273" height="654" alt="image" src="https://github.com/user-attachments/assets/8a3137a0-0dfe-4fe8-8c26-a0f07e583779" />


---

## Folder Structure

```text
Day_03_SQLAlchemy_Database_Integration/
│
├── alembic/
│   ├── versions/
│   └── ...
│
├── app/
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   ├── crud.py
│   ├── main.py
│   │
│   └── routers/
│       ├── users.py
│       └── posts.py
│
├── alembic.ini
├── requirements.txt
└── README.md
```

---

## How to Run

### 1. Create a Virtual Environment

```bash
python -m venv venv
```

### 2. Activate the Virtual Environment

**Windows (PowerShell)**

```powershell
venv\Scripts\activate
```

### 3. Install Dependencies

```bash
pip install -r requirements.txt
```

### 4. Configure PostgreSQL

Update the database URL inside `database.py` and `alembic.ini`.

Example:

```text
postgresql://username:password@localhost/database_name
```

### 5. Run Alembic Migrations

```bash
alembic upgrade head
```

### 6. Start the FastAPI Server

```bash
uvicorn app.main:app --reload
```

### 7. Open Swagger UI

```
http://127.0.0.1:8000/docs
```

---
